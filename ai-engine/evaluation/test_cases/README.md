# Evaluation Test Cases

Store sanitized, non-production artifacts here for model benchmark cases. Every case must document expected schema shape and evidence requirements; never commit secrets or private repository content.